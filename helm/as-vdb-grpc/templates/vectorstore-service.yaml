{{- if .Values.vectorstore.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "as-vdb-grpc.fullname" . }}-vectorstore
  namespace: {{ include "as-vdb-grpc.namespace" . }}
  labels:
    {{- include "as-vdb-grpc.labels" . | nindent 4 }}
    app.kubernetes.io/component: vectorstore
spec:
  type: {{ .Values.vectorstore.service.type }}
  ports:
    - port: {{ .Values.vectorstore.service.port }}
      targetPort: {{ .Values.vectorstore.service.targetPort }}
      protocol: TCP
      name: grpc
  selector:
    {{- include "as-vdb-grpc.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: vectorstore
{{- end }}